import CartTestComponent from '@/context/CartContext';
import React from 'react';

const page = () => {
    return (
        <div>
            <CartTestComponent></CartTestComponent>
        </div>
    );
};

export default page;